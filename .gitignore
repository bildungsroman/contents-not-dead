# Dependencies
node_modules
.next
.DS_STORE

# TypeScript
*.tsbuildinfo

# Local planning docs and tooling
instructions.md
wireframe.png
.cursor/

# Projects
.vercel

.projects/cache
.projects/vault
.projects/state.test.json
.projects/state.local.test.json
.env
.env.*
!.env.example

# Ignore the rest of .projects/ too: state.json and state.local.json record
# live resource and variable IDs belonging to whoever provisioned them.
# This sits below the block above deliberately — the Projects CLI rewrites
# that block and drops anything it doesn't recognize.
.projects/
