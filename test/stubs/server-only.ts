/**
 * Test stub for the `server-only` package, which throws when imported outside
 * a React Server Component. The modules under test are server-only by design;
 * this lets Vitest import them without pulling in a React server runtime.
 */
export {};
